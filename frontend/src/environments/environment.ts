export const environment = {
  production: true,
  /** Base URL for the VISTA backend API, without trailing slash.
   *  Empty string = same origin (reverse proxy or SSR server in front). */
  apiBaseUrl: '',
};
